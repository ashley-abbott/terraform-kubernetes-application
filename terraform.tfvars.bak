app_name = "nginx"
service_labels = {
  
}
service_metadata = [{
  name      = "nginx"
  labels = {
    app = "nginx"
  }
  annotations = {
    "cloud.google.com/neg" = "{\"ingress\": true}"
  }
}]

deployment_metadata = [{
  name      = "nginx"
  labels = {
    app = "nginx"
  }
}]

service_spec = [{
  type = "LoadBalancer"
  ports = [
    {
      "port"        = 8080
    },
    {
      "name"     = "something"
      "port"     = 9000,
      "target_port" = 80,
      "protocol" = "UDP"
    }
  ]
}]

common_labels = {
  dept = "FOUNDRY"
  team = "SRS"
  component = "committer"
}
